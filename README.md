# Anima Documentation

Documentation for the Anima platform — unified identity infrastructure for AI agents.

Docs site: https://docs.useanima.sh

## Contributing

1. Edit or add `.mdx` files in `docs/`.
2. Add the page to the navigation in [`docs/docs.json`](docs/docs.json) — the single source of truth for site structure.
3. Give every page `title` and `description` frontmatter.
4. Open a pull request after verifying the rendered content.

## API Reference (openapi.json)

The API Reference tab is generated by Mintlify from [`docs/openapi.json`](docs/openapi.json) — a
committed snapshot, not a live fetch, so docs deploys never depend on the API being up.

To refresh it after API contract changes (from a checkout of the `anima` monorepo):

1. `bun run generate:openapi` — writes the spec to `docs/openapi.json` in the monorepo.
2. Copy that file over this repo's `docs/openapi.json`.
3. Open a PR. Sanity checks: exactly one server entry `https://api.useanima.sh/v1`; no
   `animalabs.ai` strings.

(Automating this with a GitHub Action is a listed follow-up.)

## Sections

- Getting Started
- Platform
- Agent Identity & Commerce
- Security
- Compliance
- Protocols
- SDK Reference
- Resources
- Legal
- Blog
